# Problem To Approach

Use this file to route from problem statement to likely lecture anchors.

## If The Problem Mentions Versions, Snapshots, Or Branching History

Start with:

- `10-temporal-data-structures.md`

Look for:

- partial persistence
- full persistence
- confluent persistence
- retroactivity
- timeline vs version-DAG distinction

## If The Problem Mentions One Best Search Tree, Rotations, Or Access Sequences

Start with:

- `12-dynamic-optimality-and-bsts.md`

Look for:

- dynamic optimality
- geometric view of BSTs
- splay trees
- greedy BST ideas

## If The Problem Mentions Memory Transfers, Cache Misses, Or Block Layout

Start with:

- `13-memory-hierarchy-and-cache-oblivious.md`

Look for:

- external-memory model
- red-blue pebble game
- hierarchical memory models
- cache-oblivious design

## If The Problem Mentions Hashing, Predecessor, Or Integer Tricks

Start with:

- `14-hashing-and-integer-structures.md`

## If The Problem Mentions RMQ, LCA, Suffix Arrays, Or String Indexing

Start with:

- `15-tree-queries-and-strings.md`

## If The Problem Mentions Succinctness Or Compressed Indexes

Start with:

- `16-succinct-structures.md`

## If The Problem Mentions Dynamic Connectivity, Trees, Or Graph Updates

Start with:

- `17-dynamic-graphs.md`

## If The Right Theme Is Unclear

Use keyword search over `10-*.md`, `11-*.md`, ..., `17-*.md` first, then fall back to `L*.md`.

Suggested terms:

- `persistent`
- `retroactive`
- `optimality`
- `BST`
- `cache`
- `memory hierarchy`
- `hash`
- `integer`
- `string`
- `graph`
- `succinct`

Then confirm the model and bound before synthesizing an answer.
