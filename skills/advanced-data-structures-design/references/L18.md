
## Page 1

TODAY: Succinct data structures II (of 2)

- compact & succinct suffix arrays/trees

- O(Tlglg T)-bit suffix array

- O(T)-bit suffix array

- succinct suffix array → tree

Compact suffix arrays/trees:

* Grossi & Vitter: [STOC 2000; S[COMP 2005] first

(1+½) | T | lg |Σ | + 2 | T | + O(T/lgT) bits

O(logΣT + loutput)·logΣT) query

- Ferragina & Manzini: [FOCS 2000; JACM 2005]

5·Hk(T)·|T| + O(T/lgT) + TΣΣ+1) Burrows-WheelerTrans

kth order empirical entropy for any fixed k

= ΣPr{w occurring}·Ho(string of successor

lw|=k = #occurrences/|T| characters of w)

= ΣPxlgPx·Px= #occ.(w×)

= #bits/char in OPT code depending on last k chars

O(|P|+ loutput)·lgΣT) query

- Sadakane: [J. Alg. 2003] large alphabets

1+Σ'·Ho(T)·|T| + O(TlglgΣt+ΣlgΣt) bits

O(PlgT + loutput)·lgΣT) query for O<E,Σ<1
## Page 2

Succinct:

- Grossi, Gupta, Vitter: [SODA 2003]

   $ H_{k}(T) \cdot |T| + O(T \lg \Sigma_{1} \cdot \frac{\lg \Sigma_{2} T}{\lg T}) \text{ bits} $

   $ O(P \lg \Sigma_{1} + \frac{\lg^{2} T}{\lg \Sigma_{1}} \lg \Sigma_{1}) \text{ query} $

- Ferragina, Manzini, Mäkinen, Navarro: [TALG 2007]

   $ H_{k}(T) \cdot |T| + O(T / \lg \Sigma_{n}) \text{ bits} $

   $ O(P + \text{output} \cdot \lg^{1 + \Sigma T}) \text{ query} + O(P) \text{ counting query} $

## Extras:

- low-space construction [Hon, Sadakane, Sung - FOCs; 2003]

  (ITILg(E)) working Hon, Lam, Sadakane, Sung, Yu - Alg. 2007]

- suffix-tree ops. like maximal common substrings [Hon & Sadakane - CPM 2002: Sadakane - TCS 2007]

- document retrieval [Sadakane - J. Disc. Alg. 2007]

- dynamic [Chan, Hon, Lam, Sadakane - T. Alg. 2007]

- implementations & experiments [...]
## Page 3

index of even suffix in TK_1

- stop recursion at level  $ l = \lg \ln \Rightarrow n_{e} = n / \lg n $

 $ \Rightarrow $ can afford naive ptr. encoding:  $ n_{e} \lg n_{e} \leq n $ bits

 $ \Rightarrow $ O(lg  $ \ln $ ) query to SA[i], given O(1)-time

is-even-suffix & even-succ & even-rank
## Page 4

s-even-suffix_{k}(i) = {0 else

    even-rank_{k} = rank_{1} in is-even-suffix

    even-succ_{k}

- trivial for SA_{k}[i] even

    - store answers for SA_{k}[i] odd

    - order by i

    - order by odd suffix

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - order by (T_{k}[SA_{k}[i]]

    - actually store these pairs, in order by value

    - assume |Σ_{i}| = 2 here

    → storing sorted array of n_{k}/2 values, 2^{k} + lgn_{k} bits each

    → store leading lg n_{k} bits of each value vi via

    →ன்று unary differential encoding: O^{lead(v_{i})}10^{lead(v_{i}) - lead(v_{i})}1...

    → n_{k}/2 1's & ≤ 2^{lg n_{k}} = n_{k} O's ⇒ ≤  $ \frac{3}{2} $ n_{k} bits [17]

    → lead(v_{i}) = rank_{0}(select_{1}(i)) - rank_{0}(select_{1}(i - 1))

    → store trailing 2^{k} bits of each vi explicitly in array

    → 2^{k} n_{k}/2 = n/2 bits

    ⇒  $ \frac{1}{2} $ n +  $ \frac{3}{2} $ n_{k} + O( $ \frac{n_{k}}{lg n_{k}} $) bits

    lg lgn(rank & select + n_{k} + 1)

 $$ \begin{array}{c} \underline{\text{Total:}}\quad\sum\limits_{k=0}^{\lg\lg n}\left(n_{k}+\frac{1}{2}n+\frac{3}{2}n_{k}+O\left(\frac{n_{k}}{\lg\lg n_{k}}\right)\right)\\ =\frac{1}{2}n\lg\lg n+5n+O\left(\frac{n}{\lg\lg n}\right)bits\end{array} $$ 

PROGRESS ~

BUT TOO BIG
## Page 5

Compact suffix array: O(n) bits

- store only  $ 1/E+1 $ levels of recursion:

 $ O_{1} \in l_{1}, Q \in l_{2}, \ldots, Q = \log n $

- represent  $ SA_{K\ell\ell} $ using  $ \hat{SA}_{(k+1)\ell} $ similarly:

- "even"  $ \rightarrow $ "divisible by  $ Q^{\ell\ell'} = \text{in } SA_{(k+1)\ell} $"

- is- $ \text{even}_k = n_{K\ell\ell} $-bit vector as before + rank_1 struct.

-  $ \text{succ}_k(i) = j $ where  $ SA_{K\ell\ell}[i] = SA_{K\ell\ell}[j] - 1 $ stored in  $ n + 2n_{K\ell\ell} + O(\frac{n_{K\ell\ell}}{2\ell\ell' \cdot n_{K\ell\ell}}) $ bits like even-succ

- except can't skip  $ Y_2 $ the values

- to compute  $ SA_{ker}[i] $:

① follow succk repeatedly until j in  $ SA_{(k+1)} $

② recurse:  $ SA_{(k+1)} $

③ multiply by  $ 2^{el} $, subtract by #steps in ①

- ≤  $ 2^{el} = \log^{2} n $ succk calls per level

⇒  $ O(\lg^{2} n \lg \lg n) = O(\lg^{2} n) $ query to  $ SA[i] $

- Space:  $ \sum_{k=0}^{1/2} \left( n_{ker} + n + 2n_{ker} + O\left(\frac{n_{ker}}{g_{kgn_{ker}}}\right) \right) $

    is-evenk succk

     $  = \left(\frac{1}{2} \varepsilon + 6 \right) n + O\left(\frac{n}{g_{kgn}}\right) $ bits

- optimizations!

- succ_{k} free at level k=0  $ \Rightarrow $  $ \sum \leq 4n_{EL} = O\left(\frac{n}{g^{EL}n}\right) $

- store is-even_{k} as succinct dictionary (with rank)

 $ \Rightarrow \lg\left(n_{KLEL}^{n_{KLEL}}\right) + o \sim n_{(k+1)EL} \lg \frac{n_{KLEL}}{n_{(k+1)EL}} + o = O\left(n^{\frac{lg\lg n}{g^{EL}n}}\right) $

 $ \Rightarrow \left(\frac{1}{E} + 1\right) \cdot n + O\left(\frac{n}{g^{EL}n}\right) $ bits of L17

OPEN: O(n) bits & o(lg^{n}) query
## Page 6

Compact suffix tree given suffix array: [Munvo, Raman, Rao-J.Alg. 2001]

- store compressed binary trie on  $ 2n+1 $ nodes in  $ 4n+1 $ bits using balanced parens. [L17]

→ "skipping the skips": can't store edge lengths - during search for P, maintain letter depth of node:

to descend ☹y:

-compute length of edge by finding

longest match between leftmost-leaf(y)

starting at letter depth of × (leaf rank + SA)

- check that P matches these letters

⇒ O((P + output) · cost for SA lookup)

→# matches via # leaves in subtree

→ enumerate k matches via leaf select+rank

(Grossi & Vitter achieve  $ O(\frac{P}{\log_{\Sigma} T} + \text{output} \cdot \log_{\Sigma} T) $

## Leafops:

leaf = ()

leaf-rank = rank( ) (here)

leaf-select = select( ) (i)

# leaves in subtree = rank( ) (matching ) of parent)

- rank( ) (here)

leftmost-leaf = leaf-select(leaf-rank(here)+1)

rightmost-leaf = leaf-select(leaf-rank(matching) - 1)
## Page 7

"Succinct" suffix tree, given suffix array [Munro, Raman, Rao: fixed]

- use suffix tree above on every 6th suffix in suffix order (keep every 6th leaf in tree)

- search in tree narrows to interval of size-6 blocks in SA:

←b→

↑ actual answer ↑ result of search

→ need to find first & last match in a block

- lookup table:

  for any b b-bit strings in sorted order

    & any ≤ b-bit query string.

      find first & last prefix match

  → O(2b^2+b) bits = O(√n) if b ≤ 1/2√gn

  - to search in a block:

    ① locate the b suffixes in T using SA

    ① read next b bits from P

      & from all b suffixes (in T)

  ② repeat ⌈^P/b^7 times

  ⇒ O(P + b) time = O(P + lg(gn) if b = O(lg(gn))

      forgotten in [MRR]

    × cost of SA query ⇒ O(P lg^3 n)

  - O(n/b) bits plus size of suffix array
## Page 8

6.851 Advanced Data Structures

Spring 2012

For information about citing these materials or our Terms of Use, visit: http://ocw.mit.edu/terms.
